# 버전을 고정합니다.
#
# 핀이 없으면 배포할 때마다 최신이 깔립니다. 코드를 하나도 안 고친 재배포가
# 어느 날 갑자기 죽고, 그때는 무엇이 바뀌었는지 알 방법이 없습니다.
# discord.py 는 Discord API 변경에 맞춰 자주 올라가는 편이라 특히 위험합니다.
#
# 올릴 때는 로컬에서 확인하고 이 파일을 고쳐 PR 로 올리십시오.
# 서버 배포는 이 파일의 해시가 바뀔 때만 재설치합니다.
#
# 2026-08-15 라즈베리파이(ARM64) Python 3.13.5 에서 확인한 조합입니다.

discord.py==2.7.1
aiohttp==3.14.3
python-dotenv==1.2.2
openai==3.1.0
